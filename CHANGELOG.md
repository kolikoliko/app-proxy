# 更新日志

本项目遵循语义化版本号。

## 未发布

- 主题色设置改为右侧下拉选择，并保留当前颜色提示。
- 设置页支持自定义桌面和开始菜单快捷方式后缀，默认改为 `-proxy`。
- 代理地址改为协议下拉框与主机端口输入框，新增 HTTPS 选项并默认使用 HTTP。
- 移除 TUN、sing-box 侧载、管理员提权和透明路由相关功能，应用代理改为按需启动模式。
- 托盘中的应用菜单改为直接使用当前代理启动目标应用。
- 新增“工具代理”页面，支持检测、应用、刷新和清除 Git 用户级全局代理。
- SOCKS5 配置写入 Git 时使用远程 DNS 解析，并对读取到的代理凭据进行遮蔽显示。

## 0.3.2 - 2026-08-05

- 增加应用级环境代理启动器，并支持创建桌面和开始菜单快捷方式。
- 改进 Microsoft Store 应用组解析、浏览器代理启动和应用图标显示。
- 增强 TUN 生命周期、更新流程和前端交互的并发安全与失败回退。

## 0.2.2 - 2026-07-21

- 更新检查和下载优先使用应用中配置的代理；代理端口不可用时自动回退直连。
- 将“检查更新”按钮移动到应用更新标题行右侧，并显示实际使用的连接方式。
- TUN 增加 DNS 反向映射及 HTTP、TLS、QUIC 域名嗅探，使 Clash/Mihomo 规则模式能够识别域名规则。
- 域名嗅探仅应用于已启用的应用规则，避免影响未选择的程序。

## 0.2.1 - 2026-07-21

- sing-box 改为无控制台窗口的后台进程，开启 TUN 时不再显示黑色窗口。
- 为普通桌面应用增加可执行文件名兜底规则，修复安装路径与系统报告路径不一致时未命中代理的问题。
- 在代理设置中提示 Clash/Mihomo 使用全局模式，避免上游规则再次将部分流量分配为 DIRECT。

## 0.2.0 - 2026-07-21

- 新增签名的 GitHub 应用内更新，可手动检查、下载并安装新版本。
- 更新前安全停止 TUN，安装失败时尝试恢复原有网络状态。

## 0.1.0 - 2026-07-20

首个公开版本：

- Tauri 2 + React Windows 桌面界面、系统托盘和浅色/深色主题。
- 自定义 SOCKS5/HTTP 地址与端口、连通性检查和协议提示。
- sing-box 1.13.12 TUN 分流，每个应用独立控制代理状态。
- Windows 已安装应用扫描、原生图标和 Microsoft Store/MSIX 应用组。
- 开机自启、定时暂停、局域网绕过和安全退出。
- Windows Job Object 进程守护，避免异常退出后残留 TUN。
