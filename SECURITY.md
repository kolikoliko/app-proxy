# 安全策略

## 支持版本

当前仅维护最新的 GitHub Release。安全修复可能不会回移到更早版本。

## 报告漏洞

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私下提交报告，不要在公开 Issue 中披露可导致权限提升、流量泄漏、路由残留、任意进程代理或代理凭据泄露的问题。

报告中请包含受影响版本、Windows 版本、复现步骤、预期影响和可用的修复建议。请移除代理凭据、访问历史、真实用户目录及其他个人信息。

## 安全边界

- 程序只连接用户配置的本地 SOCKS5/HTTP 上游，不提供代理节点。
- 开启 TUN 需要 Windows 管理员权限。
- 配置和日志保存在本机，不应包含 HTTPS 明文内容。
- 独立启动器保存在当前用户的本地应用数据目录，只记录目标程序路径和代理地址；为避免落盘凭据，不允许为带账号密码的代理地址生成启动器。
- 正式安装包发布时会同时提供 SHA-256 校验值；首发安装包尚未进行商业代码签名。
